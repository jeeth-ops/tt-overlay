{
  "name": "clipper-helper",
  "version": "5.1.0",
  "description": "Offline-first local vMix clip-cutter: HIGHLIGHTS press -> exact 20s clip (15s before / 5s after) cut from the vMix recording with NO internet, filed into per-match Highlights/Batsmen/Bowlers folders, then synced to R2 + Drive + the website from a persistent queue whenever the connection is there",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "pkg server.js --targets node18-win-x64 --output ClipperHelper.exe"
  },
  "dependencies": {
    "express": "^4.18.2",
    "@ffmpeg-installer/ffmpeg": "^1.1.0"
  },
  "pkg": {
    "assets": [
      "node_modules/@ffmpeg-installer/**/*"
    ]
  }
}
