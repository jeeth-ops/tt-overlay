{
  "name": "clipper-helper",
  "version": "4.3.0",
  "description": "Local vMix clip-cutter: HIGHLIGHTS press -> exact 18s clip (15s before / 3s after) from the vMix recording, with a persistent cut queue and a separate, retrying upload queue \u2014 built for 6-7 hour matches",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "pkg server.js --targets node18-win-x64 --output ClipperHelper.exe"
  },
  "dependencies": {
    "express": "^4.18.2",
    "@ffmpeg-installer/ffmpeg": "^1.1.0"
  },
  "pkg": {
    "assets": [
      "node_modules/@ffmpeg-installer/**/*"
    ]
  }
}
