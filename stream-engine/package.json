{
  "name": "allsportslive-stream-engine",
  "version": "1.0.0",
  "description": "Local (operator PC) companion service for AllSportsLive Live Studio — NVENC encode + YouTube RTMPS push. Runs next to the Cricket Panel, never on Render.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "puppeteer-core": "^23.0.0"
  }
}
